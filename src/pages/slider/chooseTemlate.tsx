import React from "react";

import CreateWrapper from "../../components/createWrapper";

interface IProps {}

const ChooseTemplate: React.FC<IProps> = () => {
  return <CreateWrapper>Choose template</CreateWrapper>;
};

export default ChooseTemplate;
